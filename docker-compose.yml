version: '3.8'

services:
  kiro-go:
    build: .
    volumes:
      - kiro-data:/app/data
    environment:
      - CONFIG_PATH=/app/data/config.json
    restart: unless-stopped

volumes:
  kiro-data:
    driver: local
